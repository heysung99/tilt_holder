{
  "version": 2,
  "buildCommand": "pnpm --filter @workspace/tilt-holder run build && node -e \"const fs=require('fs'); fs.cpSync('artifacts/tilt-holder/dist', 'dist', {recursive: true})\"",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://tilt-holder-api-server.vercel.app/api/$1"
    }
  ]
}
