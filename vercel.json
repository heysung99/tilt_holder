{
  "version": 2,
  "buildCommand": "pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/tilt-holder run build && node -e \"const fs=require('fs'); fs.cpSync('artifacts/tilt-holder/dist', 'dist', {recursive: true})\"",
  "outputDirectory": "dist",
  "functions": {
    "api/index.mjs": {
      "includeFiles": "artifacts/api-server/dist/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ]
}
