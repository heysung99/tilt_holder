{
  "version": 2,
  "buildCommand": "pnpm build && node -e \"const fs=require('fs'); fs.cpSync('artifacts/tilt-holder/dist', 'dist', {recursive: true})\"",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ]
}
