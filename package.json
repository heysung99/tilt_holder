{
  "name": "workspace",
  "version": "0.0.0",
  "packageManager": "pnpm@9.15.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm --filter @workspace/db run build && pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/tilt-holder run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm --filter @workspace/api-server --filter @workspace/tilt-holder --if-present run typecheck",
    "dev:server": "PORT=5000 pnpm --filter @workspace/api-server run dev",
    "dev:client": "pnpm --filter @workspace/tilt-holder run dev"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
